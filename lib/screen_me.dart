

import 'package:flutter/material.dart';

class ScreenMe extends StatelessWidget {
  const ScreenMe({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Home'));
  }
}